问题 ： mysql_close_xxj(); 不会执行到
解决 ： 可以用信号钩子函数 解决 （未进行）

注意事项： 管理员权限执行 sudo ./server 
注意事项： 删除没有判断是否成功，默认成功， 客户端一定退出，但是服务端会有错误提示，

代办： 扩展
代办： makefile 语法 （模块化开发）；
代办： 回顾文件操作 和
代办： 账号可以被顶掉，很正常qq也可以，但是被顶掉的客户端，并没有退出，可以发送消息，但不能接受消息

封装函数原则，尽量不要用全局变量， 只用参数就能完成功能， 服务端是这样的， 但是客户端每处理好
基础功能已经完成

扩展功能：  聊天记录 只需要统计群聊和私聊的信息，写入一个txt文件就好
管理权限 ： 群聊下 创始人 


7.2晚
整理项目目录，调整Makefile文件，让项目结构更完善，储存聊天记录